#!/usr/bin/env bash
# Roda build (tsc -b && vite build) antes de aceitar o commit.
# Aborta se o build falhar. Bypass emergencial: git commit --no-verify.

set -e

cd "$(git rev-parse --show-toplevel)/pxg_app"

echo "[pre-commit] Running build..."
npm run build

echo "[pre-commit] Build OK"
